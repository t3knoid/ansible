# 📖 Playbook: python/compile_python3.yml

## 🛠 Purpose
Compile and install Python3 on pvenodes hosts. This is necessary for Proxmox VE nodes where a newer Python version is required but not available via standard package repositories.

## 🔗 Roles Applied
_No roles detected._

## 🚀 Usage
```bash
ansible-playbook playbooks/python/compile_python3.yml
```