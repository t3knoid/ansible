---

argument_specs:
  # roles/certbot/tasks/main.yml entry point
  main:
    short_description: Main entry point for the certbot role
    description:
      - This is the main entrypoint for the certbot role.
      - It deploys certbot following instructions documented in https://certbot.eff.org/instructions?ws=nginx&os=pip.
    author:
      - Frank Refol
