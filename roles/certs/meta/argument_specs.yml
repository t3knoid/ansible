---

argument_specs:
  # roles/certs/tasks/main.yml entry point
  main:
    short_description: Main entry point for the certs role
    description:
      - This is the main entrypoint for the certs role.
      - It creates certificates using certbot.
    author:
      - Frank Refol
