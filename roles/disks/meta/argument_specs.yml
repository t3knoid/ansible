---

argument_specs:
  # roles/disks/tasks/main.yml entry point
  main:
    short_description: Main entry point for the disks role
    description:
      - This is the main entrypoint for the disks role.
      - It prepares an attached disk by formatting and mounting to a defined mountpoint.
    author:
      - Frank Refol
