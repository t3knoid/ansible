---

- name: Remove Python modules
  ansible.builtin.pip:
    name: "{{ item }}"
    virtualenv: "/opt/python_{{ python3_version }}"
    state: absent
    umask: "0022"
  loop: "{{ ansible_setup_python_modules }}"
