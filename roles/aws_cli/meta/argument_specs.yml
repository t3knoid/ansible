---

argument_specs:
  # roles/aws_cli/tasks/main.yml entry point
  main:
    short_description: Main entry point for the aws_cli role
    description:
      - This is the main entrypoint for the aws_cli role.
      - It installs and configures AWS CLI.
    author:
      - Frank Refol

