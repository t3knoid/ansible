---

argument_specs:
  # roles/cloudinit/tasks/main.yml entry point
  main:
    short_description: Main entry point for the cloudinit role
    description:
      - This is the main entrypoint for the cloudinit role.
      - It creates a cloud-init virtual machine template.
    author:
      - Frank Refol
