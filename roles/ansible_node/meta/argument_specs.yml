---

argument_specs:
  # roles/ansible_node/tasks/main.yml entry point
  main:
    short_description: Main entry point for the ansible_node role
    description:
      - This is the main entrypoint for the ansible_node role.
      - It ensures that the ansible group is available is part of the sudoers group.
    author:
      - Frank Refol
