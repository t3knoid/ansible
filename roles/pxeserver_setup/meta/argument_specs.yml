---

argument_specs:
  # roles/pxe_server_setup/tasks/main.yml entry point
  main:
    short_description: Main entry point for the pxe_server_setup role
    description:
      - This is the main entrypoint for the pxe_server_setup role.
      - It installs and configures a PXE server.
    author:
      - Frank Refol

