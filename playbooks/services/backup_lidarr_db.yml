---
- name: Backup Lidarr database to NFS share
  hosts: pgdb
  become: true
  roles:
    - global

  tasks:

    - name: Import database_backup.yml from lidarr_setup role
      ansible.builtin.import_role:
        name: lidarr_setup
        tasks_from: database_backup.yml