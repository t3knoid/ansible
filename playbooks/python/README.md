# 📚 Playbooks in `python`

| Playbook | Purpose |
|----------|---------|
| [`compile_python3.yml`](compile_python3.md) | Compile and install Python3 on pvenodes hosts. This is necessary for Proxmox VE nodes where a newer Python version is required but not available via standard package repositories. |
| [`deploy_python3.yml`](deploy_python3.md) | Deploy Python3 on python hosts. |
| [`deploy_python3_venv.yml`](deploy_python3_venv.md) | Deploy Python3 virtual environment on python hosts. |