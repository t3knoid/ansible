---

- name: Install Python3 Environment
  hosts: python
  gather_facts: true
  become: true
  tasks:
    - name: Import venv.yml from python3 role
      ansible.builtin.import_role:
        name: python3
        tasks_from: venv.yml
