---

- name: Add ansible user
  hosts: all
  gather_facts: true

  tasks:
    - name: Import ansible role
      ansible.builtin.import_role:
        name: ansible_setup
        tasks_from: ansible_user
