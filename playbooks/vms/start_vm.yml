---
- name: Stop a VM
  hosts: pvenodes,template
  gather_facts: false
  roles:
    - global

  tasks:
    - name: Start vm
      ansible.builtin.import_role:
        name: vms
        tasks_from: start
