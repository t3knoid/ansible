---
- name: Update Pi-hole local DNS
  hosts: all
  gather_facts: false

  tasks:
    - name: Import pihole role
      ansible.builtin.import_role:
        name: pihole
        tasks_from: update_local_dns
