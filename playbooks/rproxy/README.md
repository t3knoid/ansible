# 📚 Playbooks in `rproxy`

| Playbook | Purpose |
|----------|---------|
| [`config_rproxy.yml`](config_rproxy.md) | Configures Reverse Proxy for specified sites. rproxy_setup_sites variable should be defined in the inventory or host_vars. |
| [`deploy_rproxy.yml`](deploy_rproxy.md) | Sets up Reverse Proxy on rproxy hosts. |